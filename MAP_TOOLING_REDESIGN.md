VERDICT — HARDEN THE EXISTING MACHINERY, DON'T REPLACE IT. The code-verified spine: app.py already contains the professional pattern (/api/mapdata, app.py:5498-5535 — 3 Overpass mirrors, 3600s TTL cache, stale-serve on total failure, honest 503). The redesign extends that pattern to everything still calling upstreams raw, with telemetry and concurrency protection built in — and refuses every rewrite the council unanimously rejected. The idea (one tool, three doors; community knowledge compounding) is untouched.

(a) RENDERING — Stay on Leaflet raster. MapLibre GL / PMTiles / Protomaps: REFUSED pending real evidence (user complaint, measured mobile perf problem, or Search Console signal). One same-day change: the anonymous basemaps.cartocdn.com usage is a licensing exposure on a commercial site — swap the tile URL within Leaflet. Named candidate: Stadia Maps free tier (~200k credits/mo, no credit card on file so the bill physically cannot spiral, domain-keyed key; live ToS check at implementation is the gate), with OSM standard tiles as interim fallback under the OSMF usage policy.

(b) DATA LAYER — Route road-trip.html's raw client-side Overpass calls through the /api/mapdata proxy pattern at full strength (3-mirror hedge + cache + stale-serve + honest error). The specific kill target: the whole-route rest-stop sweep at ~line 500 whose failure the false "/* reported below */" comment hides (the break-point search already surfaces failure — verified narrow claim). Nominatim geocoding: proxy it too, but honestly labeled WEAKER — cache + 1 req/s rate-limit + stale-fallback against a single upstream; no free multi-mirror ecosystem exists for geocoding, and signup-key vendors (LocationIQ/Geoapify) are refused under the no-spiral rule.

(c) ROUTING — Same weaker-tier proxy for router.project-osrm.org: aggressive route caching (routes between 46 fixed curated destinations are highly cacheable), rate-limit, stale-serve, labeled "no SLA." Self-hosted OSRM/Valhalla on Render: REFUSED (RAM/cost gold-plating at current traffic). OpenRouteService signup kept as the named fallback ONLY if telemetry proves the demo server unreliable — and its free-tier cap (council figures disagree: 2,000 vs 2,500 req/day) must be re-checked live and dated before it sizes anything.

(d) ARCHITECTURE — No build step, no framework, no big-bang rewrite. Extract JS from the 231KB monolith into plain ES modules (<script type="module">) one subsystem at a time, starting with the fetch layer — the proxy work already forces touching it. Flask/Jinja stays the shell. Extraction happens as-touched, never as a standalone project.

(e) COMPOUNDING ASSET (subsumes ADVERTISING_SPRINT C6) — Flask route /place/<slug> server-rendering each destination from destinations.json with JSON-LD (TouristAttraction + AggregateRating from community ratings + review markup from comments) and an auto-generated sitemap.xml. GATE: destinations.json (46 entries verified — the task's "47" has already drifted; compute all counts from the file at generation time, never hand-type) has NO id/slug field — adding one is a small Sean-gated schema change, slugs generated once from name and frozen forever. Every new comment/rating then becomes crawlable, AI-readable search inventory automatically.

(f) RELIABILITY — Every proxy returns a typed envelope {ok, source: live|cache|stale, fetched_at, error}; the client renders a visible banner ("showing saved results from N min ago") — silent empties die as a class. Telemetry ships WITH each proxy, not after: hit/miss/stale/timeout counters, a /api/health-map endpoint, one daily summary log line Sean can grep in Render. Phase-0 concurrency guard: a semaphore (~4 concurrent upstream fetches) + 25s per-attempt / 60s total deadline, because the current worst case (3 mirrors × ~65s ≈ 195s) exceeds gunicorn's --timeout 120 under --workers 1 --threads 8 — a worker kill drops ALL in-flight threads including booking/dispatch traffic. (Referee caveat carried honestly: the exact gthread SIGKILL semantics were asserted, not demonstrated — the cap is correct regardless.) This also resolves the OVERPASS_CAP_MS=60000 vs "30-70s normal" contradiction.

(g) BUILD ORDER — Phase 0 (same day, ~half day): CARTO tile swap + semaphore/deadline + timeout-contradiction fix. Phase 1 (2-4 days): road-trip Overpass proxy at full strength; OSRM + Nominatim proxies at labeled weaker strength; telemetry + client error surfaces shipped with each. Phase 2 (2-3 days, after Sean approves the slug field): /place/<slug> pages + JSON-LD + sitemap = C6 done. Phase 3 (ongoing): extract fetch-layer modules as each phase touches them. EXPLICITLY REFUSED as gold-plating: MapLibre, vector-tile self-hosting, self-hosted routing engines, offline/PWA capability, any credit-card-backed API key, build tooling, monolith rewrite, per-place pages before the slug gate clears.

---

**Testable claim (graded 45d, confidence 0.6):** Within 45 days, the hardened map proxies are live and gradeable from Render logs plus the new /api/health-map endpoint: (1) shipped road-trip.html contains zero raw client-side calls to overpass-api.de, router.project-osrm.org, or nominatim.openstreetmap.org (all routed through Flask proxies); (2) telemetry counters show every upstream failure produced either a stale-serve or a labeled error response — zero silent-empty results; (3) zero gunicorn "WORKER TIMEOUT" kill entries attributable to map-data routes appear in Render logs over the final 14 days of the window.
