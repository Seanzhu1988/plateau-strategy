flask>=3.0
requests>=2.31
# Verifies Google sign-in tokens against Google's signing keys, server-side.
# Pure-Python wheels, nothing to compile.
google-auth>=2.30
python-dotenv>=1.0
gunicorn>=21.0

# AES-GCM for name_vault.py, which encrypts customer names at rest. It was
# already present as a transitive dependency of google-auth, which is not the
# same as being guaranteed: pinned here because we now depend on it directly,
# and a privacy feature must not vanish because somebody else's package tree
# changed.
cryptography>=42.0
